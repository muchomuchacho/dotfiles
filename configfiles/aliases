#
# Aliases
#

# Unity
alias nunity='open -na Unity'

# Polybar
alias tpol='polybar -l info -c ~/.config/polybar/config-top.ini orion'

# Personal
## exa -> https://github.com/ogham/exa
alias l='exa -lg'
alias ll='exa -lag'
alias lt='exa -T'
alias ffe='fzf_find_edit'
alias gll='fzf_git_log'

# misc
alias edit='docker run -ti --rm -v $(pwd):/tmp docvim'
alias vim='nvim'
alias n='vim -c VimwikiIndex'
alias cat='bat'
alias ping='prettyping --nolegend'
alias du='ncdu --color dark -rr -x --exclude .git'
alias homevpn='sshuttle --dns -r homevpn 0/0'
alias plex='ssh plex'
alias xenial='ssh xenial'
alias zeus='ssh zeus'
